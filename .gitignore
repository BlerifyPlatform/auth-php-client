/vendor/
/.phpunit.cache/
/.phpunit.result.cache
composer.lock

# Real service-account credentials must never be committed; only the example template is tracked.
/config/*.json
!/config/credentials.example.json
